# Player props: owner setup and controlled release

The latest [progressive availability amendment](../governance/2026-09-15-progressive-player-props.md) authorizes a real partial initial menu and automatic first publication into approved empty slots before each target game cutoff. Its additive 1.5 / Product Bible 3.4 package preserves 1.4 and earlier history. The owner reviews current available choices plus that continuation policy; unknown future players are not represented as manually reviewed. Earlier complete-menu release instructions below apply to the older 1.4 path.

## Current approved pilot

The owner approved the [restricted nflverse primary pilot](../governance/2026-09-15-nflverse-primary-pilot.md):
each team's QB with the highest standard passing line, RB with the highest
standard rushing line and WR/TE with the highest standard receiving line, using
The Odds API and DraftKings. `FEATURED_HIGHEST_STANDARD_LINES` selects featured
players from pregame markets; it does not claim they are confirmed starters.
A QB rushing market cannot fill the RB slot. Current roster mappings verify
position and team; no depth-chart acquisition is required. The immutable
competitive package remains Ruleset 1.4 / Product Bible 3.3. `NFLVERSE_PRIMARY` and its audited nomination policy replace the
API-Sports dependency only for this explicitly configured mode.

The owner accepts overnight results, with missing evidence pending for verified
exception handling. No additional statistics subscription, nflverse key or
SportsGameOdds integration is required. The [pilot runbook](nflverse-primary-pilot.md)
records the source, timing, exception and release gates.

The owner has already approved reviewed additive migrations, merge, compatible
deployment, configuration, conditional scoped activation and compatible recovery.
Do not repeat that blanket approval. Purchases remain excluded, and actual
source validation, commissioner menu review and pregame guards remain required.
Implementation or a successful Preview is not evidence that live offers are on.

## Completed setup and remaining owner action

| Item                          | Current state                                                                                                                             | Remaining action                                                                                                                                          |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Odds API subscription and key | Owner setup and secure 20K entitlement verification completed; protected application budget transition completed without resetting usage. | Agent refreshes normalized entitlement evidence before guarded operations. No owner purchase or key setup remains.                                        |
| nflverse statistics           | Public current-season data was downloaded and validated; published license attribution is retained.                                       | Agent completes the new source-policy implementation, tests, hosted delivery and real menu checks. No account or key is required.                         |
| API-Sports                    | Its free-key account check succeeded, but current-season access was restricted.                                                           | No action for `NFLVERSE_PRIMARY`; the earlier unsent support request is not a launch dependency for this mode.                                            |
| Release approval              | Retained for the reviewed implementation and conditional scoped rollout.                                                                  | Agent completes gates and records exact PR, tested head, deployment and migration parity.                                                                 |
| Commissioner menu review      | Still required for the actual proposed slate.                                                                                             | Commissioner reviews the complete real menu and any flagged exceptions through authenticated controls after it is prepared. Fixtures are not this review. |

The existing odds subscription is already the paid capacity source. Additional
statistics and hosting purchases are not established as necessary. The existing
Supabase five-minute cron, pg_net and Vault path is installed, and the Vercel
Hobby Fluid deployment accepted the requested 120-second action duration.
Verify delivery on the new exact deployment; do not infer it from earlier builds.

## Already-open Week 2

Follow the [Week 2 amendment](../governance/2026-09-15-week2-props-cutover.md)
and [operator runbook](week2-props-cutover.md). The designated independently
authorized reset has completed, retaining original receipts, recorded
cancellation, replacement generation and the original 1,000-credit allocation.
It must not execute again. Actual participant identifiers and receipt manifests
belong only in private execution evidence.

Later props cutover can reuse that exact reset only while the unchanged binding,
empty replacement generation, absence of any new accepted week picks and all
published-game pregame guards still pass. It must freeze the complete reviewed
menu in the same transaction as the scoped 1.4 adoption. No ordinary future-week
acquisition hold applies to this already-open week. If a new bet or closed cutoff
blocks the exception, preserve that play and report the limitation.

## Ordinary future-week sequence

1. Refresh current main and preserve the owner's merged scheduled-day filters,
   historical matchups and reset support. Finish final-head lint/type/unit,
   migration/pgTAP, native concurrency and real Auth desktop/mobile gates.
   Inspect the isolated Preview and retain the tested deployment identity.
2. Apply only reviewed unapplied additive migrations in order; verify hosted
   version/content parity. Do not modify applied migrations or the migration
   journal. The original PR #51 support is already installed. Deploy compatible
   code with new offers disabled.
3. Reuse secure existing Odds API, Supabase and dispatcher settings. Select and
   validate the explicit nflverse source/selection policy. No API-Sports key is
   needed for this mode. Preview must not inherit a writable Production backend.
4. Refresh odds entitlement evidence when required. Preserve the verified
   1,000/day and 5,000/month application limits, with 350/day and 2,000/month
   protected for core lines/results. Preserve usage and actual reset accounting.
5. Configure and verify shared catalog acquisition and the existing five-minute
   dispatcher. The ordinary `scripts/player-props/catalog-setup.sql` helper
   targets the current active LIVE season and its first eligible unopened week.
   After preceding-week FINAL, publication keeps the held slate PLANNED under
   its inherited rules while automatic catalog work runs. Do not open it before
   source readiness and menu review. Do not use this hold for already-open Week 2.
6. Validate current roster/game/bookmaker mappings and the selected-player
   result path. Explicit numeric zero, negative values, verified zero offense,
   missing rows, partial game evidence and corrections must remain distinct.
   Establish an executable verified-exception path; record attribution and
   source revisions. Enable only genuinely validated source flags.
7. Enable result processing for that policy and verify actual scheduler delivery,
   deduplication, retry boundaries and incidents. Do not enable or assert
   API-Sports validation to satisfy an nflverse-only launch.
8. Record a readiness manifest and use `scripts/player-props/activate.sql` under
   the same season locks as week opening. Use the exact immutable LIVE 1.4/3.3
   package for the eligible future pilot scope. Review the real full menu once,
   confirm any permitted exceptions and open through the existing authority.
   The first accepted bet freezes identities; later prices address those players.
9. Verify ordinary lines, mixed submissions/recovery, reliable-start privacy,
   pending results, history and quota protection. Measure real hosted result
   timing after genuine accepted props finish. Do not place synthetic bets in
   the live league.

If a future held week cannot become ready, the approved
`scripts/player-props/abort-catalog-hold.sql` may release only its exact untouched
PLANNED scope for ordinary 1.3 game-only play. It rejects accepted receipts,
activated props, stale targets and closed entry windows. It is not a Week 2
reset or a way to repin an opened week.

## Compatible disable and evidence limits

`scripts/player-props/disable.sql` stops new offers and acceptance while
preserving supported rules, accepted-prop processing, reveal, corrections,
receipts and history. Keep a compatible application deployed; an old binary
that cannot read prop receipts is not a safe rollback. Do not reactivate canceled
bets or recalculate completed weeks as part of adopting the new policy.

The hosted UI Preview is labeled fictional with submission disabled. Native
Supabase/Auth/browser checks prove application behavior, not actual future-menu
review, complete inactive coverage or measured hosted latency. Overnight is the
approved expectation, not a tested guarantee. Physical-device certification is
not claimed; the owner need not perform device work unless a specific remaining
risk requires it.
